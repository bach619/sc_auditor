[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    requires_docker: test requires running Docker services (skip in CI unit-test phase)
    unit: pure unit test (no external dependencies)
    integration: integration test (requires running services)
    e2e: end-to-end pipeline test
    benchmark: performance benchmark test
