name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-python:
    name: Lint Python
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install ruff
        run: pip install ruff
      - name: Run ruff linter
        run: ruff check services/*/src/ --no-fix

  lint-frontend:
    name: Lint Frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install dependencies
        run: |
          cd services/15-dashboard/frontend
          npm ci
      - name: Run ESLint
        run: |
          cd services/15-dashboard/frontend
          npx eslint src/ --no-error-on-unmatched-pattern

  type-check:
    name: TypeScript Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install dependencies
        run: |
          cd services/15-dashboard/frontend
          npm ci
      - name: Run TypeScript check
        run: |
          cd services/15-dashboard/frontend
          npx tsc --noEmit

  test-backend:
    name: Backend Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest pytest-asyncio pytest-timeout
      - name: Run tests
        run: |
          python -m pytest tests/ -v --timeout=30 -x

  validate-docker:
    name: Validate Docker Compose
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate docker-compose.yml
        run: python3 -c "import yaml; d=yaml.safe_load(open('docker-compose.yml')); svc=len(d.get('services',{})); assert svc>=20; print('OK:', svc, 'services')"
